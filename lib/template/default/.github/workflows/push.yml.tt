name: Push Up
on:
  push:
    branches:
      - main
      - master

jobs:
  run:
    runs-on: ubuntu-latest

    env:
<%= cloud_env_vars(indent: 6) -%>
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      TS_ENV: dev
      TS_TOKEN: ${{ secrets.TS_TOKEN }}

    steps:
<%= partial "init_steps.yml" -%>

    - name: Terraspace Command
      run: |
        terraspace up demo -y
